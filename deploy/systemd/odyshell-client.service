[Unit]
Description=Odyshell client
After=network-online.target docker.service
Wants=network-online.target

[Service]
Type=simple
User=odyshell
Group=odyshell
ExecStart=/usr/local/bin/odyshell-client start --config /etc/odyshell/client.json
Restart=always
RestartSec=3
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=read-only
ReadWritePaths=/var/lib/odyshell

[Install]
WantedBy=multi-user.target
