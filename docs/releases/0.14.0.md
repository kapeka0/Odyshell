# Odyshell 0.14.0

Odyshell 0.14.0 adds manually approved Host Shell Sessions and explicit, per-Profile sudo access
for Linux Clients.

- Agents can request temporary Host Shell authority without predicting every command up front.
- Every Host Shell request requires manual approval and is bound to one host machine and Session
  lifetime; it cannot be autoapproved or delegated.
- Approved commands run as the existing Client user, start in that user's Home by default, and may
  use any path that account can access.
- Each command runs in a fresh native login shell with bounded input, output, timeout and an
  allowlisted base environment. Host Shell does not provide a PTY or an additional sandbox.

- Sudo remains blocked by default.
- `ods profiles configure <name> --allow-sudo` enables it only when passwordless sudo already works.
- `ods profiles configure <name> --deny-sudo` restores the default restriction.
- Machine details, approvals and MCP discovery disclose when root access may be possible.
- Text areas follow the application layout without browser resize handles.

Install the release:

| Package manager | Command |
| --- | --- |
| pnpm | `pnpm add --global @odyshell/cli@0.14.0` |
| npm | `npm install --global @odyshell/cli@0.14.0` |
| Yarn | `yarn global add @odyshell/cli@0.14.0` |
| Bun | `bun add --global @odyshell/cli@0.14.0` |

To allow passwordless sudo for one Linux Profile:

```bash
ods profiles configure <name> --allow-sudo
```

Use a narrow `NOPASSWD` sudoers policy. Odyshell never stores or submits a sudo password.
